# === DEV PROFILE ===
# Verbose logging
logging.level.root=INFO
logging.level.com.skyfence=DEBUG

# Health details visible without auth
management.endpoint.health.show-details=always

# Actuator CORS - local frontends
management.endpoints.web.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# CORS - local frontends
cors.allowed-origins=http://localhost:3000,http://localhost:5173
